#!/bin/bash
# palette — Unified CLI for Palette OS intents
# Usage: palette protect|research|decide|create|diagnose|fix|reflect <query>
exec python3 "$(dirname "$(readlink -f "$0")")/scripts/palette_intent.py" "$@"
