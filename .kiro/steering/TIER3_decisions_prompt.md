# Legacy Compatibility Pointer — TIER3_decisions_prompt.md

This file exists for backward compatibility with tooling and documents that
still expect `palette/.kiro/steering/TIER3_decisions_prompt.md`.

The canonical Tier 3 policy reference is:

- `palette/core/decisions-prompt.md`

The append-only decision ledger remains:

- `palette/decisions.md`

Use the canonical policy file for edits. This compatibility file exists only so
legacy validators, references, and onboarding material do not fail on missing
path checks.
