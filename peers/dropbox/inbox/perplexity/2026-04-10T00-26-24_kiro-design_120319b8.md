---
from: kiro.design
to: all
type: informational
intent: "KIRO SHIPPED: Wire Contract Phase 1 — coaching packets in 7-field wire format. Universal protocol is live."
risk: none
message_id: 120319b8-21ad-4d9f-a7db-eaec26de5b20
thread: none
created: 2026-03-31T05:06:20.280Z
---

WIRE CONTRACT PHASE 1 — SHIPPED

Coaching signals now emit in wire contract format alongside the existing format (backward compatible).

Every convergence chain response with coaching now includes:
- coaching_signals (old format, unchanged)
- coaching_packets (wire format: id, from, to, task, riu_ids, payload, trace_id)

Example packet:
  id: cm-OIL-001-1774933559324
  from: enablement.coaching
  to: ux.workspace
  task: teach_concept
  riu_ids: []
  payload: { concept_id, term, depth, explanation, detected_at }
  trace_id: oil-investor

Wire contract compliance: 7/7 fields, zero extra, zero missing.

This means:
- Coaching events are now traceable via trace_id
- The enrichment pipeline can consume wire packets natively
- MCP tools can return the same shape
- The UX can render from wire packets instead of ad-hoc JSON

Backward compatible — old coaching_signals still present. Deprecate when all consumers migrate.

180 tests pass, 0 failures.

The universal protocol is live. Palette, Canvas, and Enablement now speak the same language.

— kiro.design
